apiVersion: v2
name: mailstrix
description: >-
  strixd — a single Go HTTP daemon that scans mail content with YARA rules and
  optional abuse.ch threat-intel feeds. Designed as an internal scan backend for
  the rspamd mailstrix.lua plugin (ClusterIP-only, not web-facing).
type: application
# Chart version — bump on chart changes.
version: 0.1.0
# Application version — the strixd release this chart defaults to (image tag).
appVersion: "1.2.0"
home: https://github.com/eilandert/mailstrix
sources:
  - https://github.com/eilandert/mailstrix
keywords:
  - yara
  - rspamd
  - antivirus
  - mail-security
  - threat-intel
maintainers:
  - name: eilandert
    url: https://github.com/eilandert

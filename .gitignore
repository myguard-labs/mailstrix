/yarad
/dist/
*.test
*.out
coverage.txt
# deploy-time secrets (token file referenced by docker-compose) — never commit
secrets/
*.token
.claude/worktrees/

# live malware sample corpus — local only, never publish
testdata/live-samples/
